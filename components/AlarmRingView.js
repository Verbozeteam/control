/* @flow */

import * as React from 'react';
import { View, Text, StyleSheet } from 'react-native';

type PropTypes = {};
type StateType = {};

export default class AlarmRingView extends React.Component<PropsType, StateType> {

  render() {
    return null;

    return (
      <View style={styles.container}>
      </View>
    );
  }
}

const styles = StyleSheet.create({
  container: {
    flex: 1
  }
});
